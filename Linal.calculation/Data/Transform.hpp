#pragma once
#include "Vector2d.h"
#include "../Util/Math.h"
#include <cmath>
#include <vector>

class Transform {
private:
	/*
	Matrix3d transformation;

	///mutable because it is a cached value, and needs to be changed in const functions
	mutable Matrix3d worldMatrix;
	///mutable because it is a cached value, and needs to be changed in const functions
	mutable bool worldMatrixValid;
	///mutable because it is a cached value, and needs to be changed in const functions
	mutable Vector3d cachedAbsolutePosition;

	GameObject& gameObject;
	std::vector<std::reference_wrapper<Transform>> children;
	NullableReference<Transform> parent;
	*/
public:

};